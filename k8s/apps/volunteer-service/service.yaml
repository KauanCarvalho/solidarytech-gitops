apiVersion: v1
kind: Service
metadata:
  name: volunteer-service
  namespace: volunteer-service
spec:
  selector:
    app: volunteer-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8083
  type: ClusterIP
